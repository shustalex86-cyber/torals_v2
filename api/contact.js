const{createClient}=require('@supabase/supabase-js');module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({error:'no'});const sb=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_KEY);const{name,phone,description}=req.body;if(!name||!phone)return res.status(400).json({error:'Имя и телефон'});await sb.from('contact_requests').insert({name,phone,description:description||''});const bt=process.env.BOT_TOKEN,ci=process.env.ADMIN_CHAT_ID;if(bt&&ci){try{await fetch('https://api.telegram.org/bot'+bt+'/sendMessage',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({chat_id:ci,text:'📩 *Заявка с сайта!*\n\n👤 '+name+'\n📞 '+phone+(description?'\n📝 '+description:''),parse_mode:'Markdown'})})}catch(e){}}res.json({ok:true})};
